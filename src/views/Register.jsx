import React from 'react';

import { RegisterForm } from '../components';

const Register = () => {
    return (
        <>
            <RegisterForm />
        </>
    );
};

export default Register;
